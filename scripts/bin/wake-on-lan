#!/usr/bin/env bash

hosts=(
    "FDW-2509"
)

declare -A macs=(
    [FDW-2509]="12:34:56:78:9A:BC"
)

echo "==== WoL Hosts ===="

PS3="Select Host: "

select host in hosts; do
    [[ -n "$host" ]] || continue

    wakeonlan "${macs[$host]}"
    break
done
