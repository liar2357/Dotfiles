#!/usr/bin/env bash

hosts=(
	"FDW-2509"
)

declare -A macs=(
	["FDW-2509"]="9c:6b:00:84:34:93"
)

echo "==== WoL Hosts ===="

PS3="Select Host: "

select host in $hosts; do
	[[ -n "$host" ]] || continue

	wakeonlan "${macs[$host]}"
	break
done
